projection;
strict ( 2 );
use draft;

define behavior for ZC_ITEM02_N alias item
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;
}